import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appstack.app',
  appName: 'AppStack',
  webDir: 'out',
  backgroundColor: '#1F2937'
};

export default config;
